"use client";
export default function IntroHeadline() {
  return (
    <section className="min-h-screen flex items-center justify-center bg-white">
      <h2
        id="wwd-intro"
        className="text-[clamp(3rem,8vw,8rem)] font-black opacity-0 translate-y-10"
      >
        What we do —
      </h2>
    </section>
  );
}
